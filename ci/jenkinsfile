pipeline {
    agent any
    stages {
        stage('Newman Tests') {
            steps {
                sh '''
                cd api/newman
                newman run proyecto.postman_collection.json -e heroku.postman_environment.json
                '''
            }
        }
    }
}

